# Mock 模块策略（2026-06-26）

产品决策记录，对应 MVP 计划 **2.1**。

| 模块 | 策略 | 说明 |
|------|------|------|
| `ai_canvas` | **Preview** | 保留导航；`ModulePreviewBanner` + paid-beta 不承诺持久化画布管线 |
| `avatar_home` / `avatar_create` / `avatar_voice` / `avatar_space` | **Preview** | 同上；Avatar B01/B02 全量单独立项 |
| `copywriting_keywords` | **Implement** | registry `implemented`；P1-R06 CRUD 已接 `keywordRepository` |

隐藏项（不变）：`e_white_bg`、`marketing_diy`。

## Cohort 文案

- **Included：** 关键词库（工作区词库 CRUD）
- **Preview only：** AI 画布、Avatar 四模块